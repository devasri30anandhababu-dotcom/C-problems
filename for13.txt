#include<stdio.h>

int main(){
    int num, n, sum = 0;

    scanf("%d%d", &num, &n);

    for(int i = 1; i <= n; i++){
        sum = sum + (num * i);
    }

    printf("%d", sum);

    return 0;
}