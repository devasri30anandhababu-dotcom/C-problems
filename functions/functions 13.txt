#include<stdio.h>
int find(int a,int b, int n){
    for(int i=1;i<=b;i++){
        n=n*a;
    }
    return n;
}

int main(){
    int a,b,n=1;
    scanf("%d%d",&a,&b);
    printf("%d",find(a,b,n));
}