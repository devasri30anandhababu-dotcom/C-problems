#include <stdio.h>
void find(int a, int b, int temp){
    while(b!=0){
        temp=b;
        b=a%b;
        a=temp;
    }
    printf("%d",a);
}
int main() {
    int a,b,temp;
    scanf("%d%d",&a,&b);
    find(a,b,temp);
    return 0;
}
