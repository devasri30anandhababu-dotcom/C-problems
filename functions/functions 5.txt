#include<stdio.h>
int digits(int n){
    int rem;
    int sum=0;
    for(int i=n;n!=0;n=n/10){
        rem=n%10;
        sum+=rem;
    }
    return sum;
}
int main(){
    int n;
    scanf("%d",&n);
    printf("%d",digits(n));
    return 0;
}
