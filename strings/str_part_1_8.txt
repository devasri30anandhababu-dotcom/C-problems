#include <stdio.h>
int main(){//aeiou
    char str[20];
    fgets(str,sizeof(str),stdin);
    int space=0;
    for(int i=0;str[i]!='\0';i++){
        if(str[i]==' '){
            space++;
        }
    }
    printf("%d",space);
    return 0;
}