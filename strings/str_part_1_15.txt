#include <stdio.h>
int main(){
    char str[50];
    int i;
    fgets(str,sizeof(str),stdin);
    int space=0;
    int word=0;
    for(int i=0;str[i]!='\0';i++){
        if(str[i]==' '){
           space++;
        }
        
    }
    printf("%d",space+1);
    return 0;
}