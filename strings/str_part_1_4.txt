#include <stdio.h>
#include<string.h>
int main(){
    int i,j;
    char str[20];
    fgets(str,sizeof(str),stdin);
    i=0;
    while(str[i]!='\0'){
        i++;
    }
    j=i-1;
    while(j>=0){
        printf("%c",str[j]);
        j--;
    }
    
    return 0;
}