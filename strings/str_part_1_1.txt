#include <stdio.h>

int main() {
    char str[20];
    fgets(str,sizeof(str),stdin);
    int length=0;
    for(int i=0;str[i]!='\0';i++){
        if(str[i]=='\n'){
            continue;
        }
        length++;
    }
    printf("%d",length);

    return 0;
}