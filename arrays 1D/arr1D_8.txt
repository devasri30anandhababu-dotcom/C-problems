
#include <stdio.h>

int main() {
    int n,k,i,j;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&k);
    for(int i=k;i<n;i++){
        
            printf("%d ",arr[i]);
        }
        for(int j=0;j<k;j++){
            printf("%d ",arr[j]);
        }
    

    return 0;
}